This module provides authentication using JWT (JSON Web Token) along with data validation using Zod. It also implements CRUD (Create, Read, Update, Delete) operations for resource management.

## Features
- Secure authentication with JWT
- Schema validation using Zod
- Basic CRUD functionality for managing resources

